# How to run
You need to edit the `WARBAND_ROOT` variable in texture_server.py to point to Warband's root directory in your filesystem.
Then run
```bash
python3 texture_server.py
```
And the MapViewer should be accesible under
[http://localhost:8791/main.html](http://localhost:8791/main.html)